.venv/
__pycache__/
*.pyc

db.sqlite3
db.sqlite3-journal
media/
staticfiles/

# Secrets — never commit
.env
.env.*
!.env.example

data/raw/
# ml/artifacts/ is intentionally NOT ignored — the quantized ONNX artifact
# ships with the repo and is HMAC-verified at runtime.

# Local AI/plugin state — never commit
.claude/
.omc/
CLAUDE.local.md

# Test artifacts + scratch files
home.html
hub.html
grey96.png
live_body*.json

# Tooling
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Editors / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
